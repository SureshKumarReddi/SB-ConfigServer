server:
  port: 9090
  
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/SureshKumarReddi/cloudconfig.git
          default-label: main
          clone-on-start: true
